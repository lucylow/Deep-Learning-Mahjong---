# Figma Make source reference

This directory preserves the downloaded MahjongArenaMobileAppDesign source for visual provenance. The files use `.source` extensions so Expo TypeScript compilation does not treat the Vite/DOM frontend as native application code. The runtime Expo screens port its tokens, layouts, tile styling, and interaction patterns.
